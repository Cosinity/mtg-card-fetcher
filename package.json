{
  "name": "mtg-card-fetcher-webhook",
  "version": "1.0.0",
  "description": "A Facebook messenger bot to fetch Magic: The Gathering card info",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node run-code/index.js",
    "postinstall": "./node_modules/.bin/babel ./build-code --out-dir run-code --ignore node_modules"
  },
  "author": "Andrew Costenoble",
  "license": "ISC",
  "babel": {
    "presets": [
      "env"
    ],
    "ignore": [
      "node_modules"
    ]
  },
  "dependencies": {
    "body-parser": "^1.18.3",
    "express": "^4.16.3",
    "request": "^2.87.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-polyfill": "^6.26.0",
    "babel-preset-env": "^1.7.0"
  }
}
